function solution(array, commands) {
  var answer = [];
  let str1;
  let str2;
  let str3;
  for(let a=0;a<commands.length;a++){
    for(let b=0;b<commands[a].length;b++){
        str1=commands[a][0]
        str2=commands[a][1]
        str3=commands[a][2]
        console.log(str1)
        console.log(str2)
        console.log(str3)
      break
    }
  }
  return answer;
}